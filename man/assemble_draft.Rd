% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fct_build.R
\name{assemble_draft}
\alias{assemble_draft}
\title{Assemble the annotated draft from survey state}
\usage{
assemble_draft(
  spec,
  answers,
  sources = list(),
  rationales = list(),
  prior_annotations = list()
)
}
\arguments{
\item{spec, answers}{As in [build_params()].}

\item{sources}{Named list, instance id -> "stated"/"benchmark"/"file".}

\item{rationales}{Named list, `<instance id>__why` -> free text.}

\item{prior_annotations}{The annotations block of a loaded file, so
its provenance survives the edit cycle; session-tracked sources
overlay it only on the leaves the user touched.}
}
\value{
An annotated parameter list ready for [write_params()].
}
\description{
[build_params()] plus the provenance layer: every answered question
whose source the survey tracked gets an annotation on its parameter
path (`stated` or `benchmark`, with the optional rationale), except
where the builder already wrote one (the anchor-set auto-annotation
wins). Answers whose source is `file` - loaded and untouched - get
no new annotation: the file already says what it says.
}
